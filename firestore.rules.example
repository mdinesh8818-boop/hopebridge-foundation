rules_version = '2';

// EXAMPLE / MIRROR — production rules live in firestore.rules
// Deploy: firebase deploy --only firestore:rules
// See docs/ACCESS_CONTROL.md

// This file intentionally points reviewers at the canonical rules file.
// Copy contents from ./firestore.rules when reviewing historical PRs that
// still reference firestore.rules.example.
