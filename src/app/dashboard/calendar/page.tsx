import ModulePage from "../components/ModulePage";

export default function CalendarPage() {
  return (
    <ModulePage
      eyebrow="Scheduling"
      title="Calendar"
      modulePath="Calendar"
      description="Coordinate events, volunteer shifts, campaign milestones, and team meetings in a unified calendar view with reminders and sync."
      primaryAction="Schedule Event"
    />
  );
}
